# Автоматическая отправка Stars через Telegram Bot API

## Проблема
NDN списывались с баланса пользователя, но Stars не приходили. Требовалось реализовать автоматическую отправку через Telegram Bot API метод `sendStars`.

## Решение

### Добавлена автоматическая отправка Stars через Bot API

```javascript
// Отправляем Stars через Telegram Bot API
const sendStarsResponse = await axios.post(`${TELEGRAM_API}/sendStars`, {
  chat_id: telegram_id, // ID пользователя в Telegram
  amount: Math.round(stars_amount) // Количество Stars для отправки
});

if (sendStarsResponse.data.ok) {
  res.json({
    success: true,
    message: `✅ ${stars_amount} Stars отправлены на ваш аккаунт в Telegram!`,
    new_balance: newBalance,
    stars_sent: stars_amount
  });
}
```

## Логика работы

### Последовательность действий:

1. **Пользователь создает заявку** → указывает количество NDN и username
2. **Проверяется баланс** → достаточно ли NDN у пользователя
3. **NDN списываются** → с баланса пользователя
4. **Создается транзакция** → в базе данных
5. **Отправляются Stars** → через `sendStars` метод Telegram Bot API
6. **Если успешно** → пользователь получает Stars
7. **Если ошибка** → баланс откатывается, транзакция удаляется

### Откат при ошибке:

```javascript
// Если отправка Stars не удалась - откатываем операцию
await supabase
  .from('nodeon_users')
  .update({ balance_ndn: user.balance_ndn })
  .eq('id', user.id);

await supabase
  .from('nodeon_transactions')
  .delete()
  .eq('id', transaction.id);
```

## Telegram Bot API Method: sendStars

### Параметры:
- `chat_id` - ID пользователя в Telegram (telegram_id)
- `amount` - Количество Stars для отправки (округленное до целого)

### Ограничения:
- Минимальная сумма: 1 Star
- Бот должен иметь достаточный баланс Stars в кошельке
- Пользователь должен начать диалог с ботом хотя бы один раз

## Курс обмена

**Тестовый режим:** 1 NDN = 1 Star (курс 1:1)
- Без комиссий
- Без лимитов
- Автоматическая отправка

## Результат

- ✅ Stars автоматически отправляются через Bot API
- ✅ Если отправка не удалась - баланс откатывается
- ✅ Пользователь получает подтверждение с суммой отправленных Stars
- ✅ Транзакция создается в базе данных
- ✅ Баланс обновляется автоматически

## Важно

### Необходимо проверить:

1. **Бот имеет Stars в кошельке** - нужно пополнить баланс бота
2. **Метод `sendStars` доступен** - бот должен поддерживать Stars
3. **Пользователь начал диалог** - хотя бы раз написал боту

### Возможные ошибки:

- `insufficient_stars_balance` - у бота недостаточно Stars
- `chat_not_found` - пользователь не начинал диалог с ботом
- `invalid_amount` - неверное количество Stars

## Тестирование

1. Откройте Mini App в Telegram
2. Обменяйте NDN на Stars
3. Проверьте что Stars пришли на ваш аккаунт
4. Проверьте что баланс NDN обновился

## Логи

```
💫 Получен запрос на обмен NDN на Stars
✅ Заявка на обмен создана: { user_id: 5, amount_ndn: 2, stars_amount: 2 }
📤 Отправляем Stars через Bot API: chat_id=207940967, amount=2
✅ Stars отправлены через Bot API: { ok: true }
```

## Следующие шаги

1. Пополнить баланс Stars у бота
2. Протестировать отправку Stars
3. Добавить обработку ошибок при недостаточном балансе
4. Добавить уведомления пользователю о статусе заявки










