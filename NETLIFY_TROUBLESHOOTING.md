# 🔧 Устранение неполадок Netlify

## 📋 Проверка статуса деплоя

### 1. Откройте Netlify Dashboard:
https://app.netlify.com/projects/nodeon-pyramid

### 2. Проверьте статус деплоя:
- Перейдите в раздел **"Deploys"**
- Убедитесь, что последний деплой имеет статус **"Published"**
- Если статус **"Failed"** - нажмите на него для просмотра ошибок

### 3. Проверьте переменные окружения:
- Перейдите в **"Site settings"** → **"Environment variables"**
- Убедитесь, что установлены:
  ```
  SUPABASE_URL = https://ahxwpzgltlzlvrtrbanm.supabase.co
  SUPABASE_ANON_KEY = eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
  TELEGRAM_BOT_TOKEN = 7670372637:AAG7XWbNkhvNx_M4MI4118AYXvIsn3bRMDQ
  DEVELOPER_TELEGRAM_ID = 207940967
  SECRET_KEY = nodeon-pyramid-secret-key-2024
  ```

## 🧪 Тестирование API

### 1. Проверка таблиц:
```bash
curl https://nodeon-pyramid.netlify.app/api/miner/check-tables
```

### 2. Проверка данных майнера:
```bash
curl https://nodeon-pyramid.netlify.app/api/miner/data/5
```

### 3. Проверка главной страницы:
Откройте: https://nodeon-pyramid.netlify.app

## 🔍 Проверка логов

### 1. Логи функций:
https://app.netlify.com/projects/nodeon-pyramid/logs/functions

### 2. Логи деплоя:
https://app.netlify.com/projects/nodeon-pyramid/deploys

## 🚨 Частые проблемы

### Проблема: API возвращает ошибки
**Решение:**
1. Проверьте переменные окружения
2. Убедитесь, что деплой завершился успешно
3. Проверьте логи функций

### Проблема: Пользователи не загружаются
**Решение:**
1. Проверьте `SUPABASE_URL` и `SUPABASE_ANON_KEY`
2. Убедитесь, что ключи правильные
3. Проверьте подключение к базе данных

### Проблема: Функции не работают
**Решение:**
1. Проверьте логи функций в Netlify Dashboard
2. Убедитесь, что все зависимости установлены
3. Проверьте синтаксис JavaScript кода

## 🔄 Перезапуск деплоя

### Через веб-интерфейс:
1. Netlify Dashboard → **"Deploys"**
2. Нажмите **"Trigger deploy"** → **"Deploy site"**

### Через CLI:
```bash
cd dist
netlify deploy --prod
```

## 📞 Поддержка

Если проблемы не решаются:
1. Проверьте логи в Netlify Dashboard
2. Убедитесь в правильности переменных окружения
3. Проверьте подключение к Supabase




