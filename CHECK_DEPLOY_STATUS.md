# 🔍 Проверка статуса деплоя

## 📋 Что нужно проверить:

### 1. Статус деплоя в Netlify Dashboard:
- Откройте: https://app.netlify.com/projects/nodeon-pyramid
- Перейдите в раздел **"Deploys"**
- Убедитесь, что последний деплой имеет статус **"Published"** (зеленый)

### 2. Переменные окружения:
- Перейдите в **"Site settings"** → **"Environment variables"**
- Убедитесь, что установлены:
  ```
  SUPABASE_URL = https://ahxwpzgltlzlvrtrbanm.supabase.co
  SUPABASE_ANON_KEY = eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
  TELEGRAM_BOT_TOKEN = 7670372637:AAG7XWbNkhvNx_M4MI4118AYXvIsn3bRMDQ
  DEVELOPER_TELEGRAM_ID = 207940967
  SECRET_KEY = nodeon-pyramid-secret-key-2024
  ```

### 3. Логи функций:
- Перейдите в **"Functions"** → **"View logs"**
- Проверьте, есть ли ошибки в логах

## 🧪 Тестирование API:

### Проверка таблиц:
```bash
curl https://nodeon-pyramid.netlify.app/api/miner/check-tables
```

### Проверка данных майнера:
```bash
curl https://nodeon-pyramid.netlify.app/api/miner/data/5
```

### Проверка главной страницы:
Откройте: https://nodeon-pyramid.netlify.app

## 🔄 Если деплой не завершился:

1. **Подождите** - деплой может занять несколько минут
2. **Проверьте логи** - в разделе "Deploys" нажмите на последний деплой
3. **Перезапустите деплой** - нажмите "Trigger deploy" → "Deploy site"

## 📊 Ожидаемый результат:

После успешного деплоя API должен:
- ✅ Возвращать статус 200 для `/api/miner/check-tables`
- ✅ Показывать существующие таблицы в Supabase
- ✅ Загружать данные майнера для пользователя 5 (Gas: 2207.42)
- ✅ Отображать главную страницу приложения

## 🚨 Если что-то не работает:

1. **Проверьте переменные окружения** - они должны быть точно такими же, как в .env файле
2. **Проверьте логи функций** - там будут детали ошибок
3. **Убедитесь, что деплой завершился** - статус должен быть "Published"




